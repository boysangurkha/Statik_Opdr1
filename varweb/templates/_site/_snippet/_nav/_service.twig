{% set serviceNav = craft.navigate.raw('serviceNav') %}
{% if serviceNav|length %}
	<nav class="" aria-label="{{ 'Service'|t }}">
		<ul class="flex flex-wrap space-x-5 pr-5">
			{% for node in serviceNav %}
				<li class="{% if node.classes|length %}{{ node.classes }}{% endif %}">
					<a href="{{ node.url }}" class="text-xxs text-pitchblack font-sans hover:underline{% if node.active %} underline{% endif %}" {% if node.blank %} target="_blank" rel="noopener" {% endif %}>{{ node.name }}
					</a>
				</li>
			{% endfor %}
		</ul>
	</nav>
{% endif %}
