{% set serviceNav = craft.navigate.raw('serviceNav') %}
{% if serviceNav|length %}
	<nav class="" aria-label="{{ 'Service'|t }}">
		<ul class="flex flex-wrap space-x-5">
			{% for node in serviceNav %}
				<li class="{% if node.classes|length %}{{ node.classes }}{% endif %}">
					<a href="{{ node.url }}" class="text-md text-black-400 hover:underline{% if node.active %} text-black underline{% endif %}" {% if node.blank %} target="_blank" rel="noopener" {% endif %}>{{ node.name }}
					</a>
				</li>
			{% endfor %}
		</ul>
	</nav>
{% endif %}
